---
- name: Wipe MinIO volume (if mounted at /mnt/minio)
  file:
    path: /mnt/minio
    state: absent

- name: Wipe other common app volumes (customize as needed)
  file:
    path: /mnt/data
    state: absent

- name: Wipe temporary workspace data
  file:
    path: /opt/apps
    state: absent
